package rutas.uacm.models.service;

public class ITipoTransporteService {
	List<TipoTransporte> obtenerTodos();
	TipoTransporte buscarId(String id);
	TipoTransporte insertar(TipoTransporte estudiante);
	void eliminar(String id);
	TipoTransporte buscarCorreo(String correo);
}
